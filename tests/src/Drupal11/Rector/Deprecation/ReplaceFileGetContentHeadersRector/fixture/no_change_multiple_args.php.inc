<?php

// Multiple arguments — not a valid call signature, should not be touched
$headers = file_get_content_headers($file, $extra);

?>
-----
<?php

// Multiple arguments — not a valid call signature, should not be touched
$headers = file_get_content_headers($file, $extra);

?>
