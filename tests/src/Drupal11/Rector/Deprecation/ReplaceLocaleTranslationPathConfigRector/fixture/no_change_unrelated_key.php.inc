<?php

// Different key on locale.settings — must not be transformed.
$pattern = \Drupal::config('locale.settings')->get('javascript.directory');
?>
-----
<?php

// Different key on locale.settings — must not be transformed.
$pattern = \Drupal::config('locale.settings')->get('javascript.directory');
?>
