<?php

// Different config name — must not be transformed.
$siteName = \Drupal::config('system.site')->get('translation.path');
?>
-----
<?php

// Different config name — must not be transformed.
$siteName = \Drupal::config('system.site')->get('translation.path');
?>
