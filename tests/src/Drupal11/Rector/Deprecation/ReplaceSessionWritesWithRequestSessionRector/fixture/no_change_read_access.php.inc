<?php

// Read access is not transformed — only Assign nodes are handled.
$value = $_SESSION['my_key'];
$exists = isset($_SESSION['my_key']);
echo $_SESSION['my_key'];

?>
-----
<?php

// Read access is not transformed — only Assign nodes are handled.
$value = $_SESSION['my_key'];
$exists = isset($_SESSION['my_key']);
echo $_SESSION['my_key'];

?>
