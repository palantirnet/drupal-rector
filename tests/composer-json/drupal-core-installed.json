{
    "_comment": "Stands in for the analysed project's composer.json in rule tests: this package does not require drupal/core, so rules bonded to it through ComposerPackageConstraintInterface would all be filtered out. The version is the highest any rule is bonded to, so every rule is active.",
    "require": {
        "drupal/core": "12.0.0"
    }
}
